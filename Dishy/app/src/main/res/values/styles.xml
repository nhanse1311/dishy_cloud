<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="BottomSheetDialog" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@drawable/custom_bottom_sheet</item>
        <item name="android:layout_marginLeft">@dimen/dp3</item>
        <item name="android:layout_marginRight">@dimen/dp3</item>
        <item name="android:layout_marginBottom">@dimen/dp1</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme_Dialog" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="android:windowMinWidthMajor">90%</item>
        <item name="android:windowMinWidthMinor">90%</item>
        <item name="bottomSheetStyle">@drawable/custom_bottom_sheet</item>
        <item name="android:layout_marginLeft">@dimen/dp3</item>
        <item name="android:layout_marginRight">@dimen/dp3</item>
        <item name="android:layout_marginBottom">@dimen/dp1</item>
        <item name="android:windowFullscreen">true</item>
    </style>
    <style name="AudioFileInfoOverlayText">
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:shadowColor">@color/colorBlack</item>
        <item name="android:shadowRadius">5</item>
    </style>

    <style name="AppTheme.FullScreen" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:fontFamily">sans-serif-light</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>
